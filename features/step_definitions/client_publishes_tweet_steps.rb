require "client"

Given /^a valid tweet$/ do
  pending # express the regexp above with the code you wish you had
end

When /^the client publishes the tweet$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^the client should return "([^"]*)"$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end
